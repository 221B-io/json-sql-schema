{
  "$id": "columns/string.schema.json",
  "$merge": {
    "source": {
      "$ref": "_base.schema.json"
    },
    "with": {
      "properties": {
        "type": {
          "type": "string",
          "enum": ["binary", "string"]
        },
        "length": {
          "type": "integer"
        }
      }
    }
  }
}
