{
  "$id": "columns/text.schema.json",
  "$merge": {
    "source": {
      "$ref": "_base.schema.json"
    },
    "with": {
      "properties": {
        "type": {
          "type": "string",
          "enum": ["text"]
        },
        "textType": {
          "type": "string",
          "enum": ["text", "mediumtext", "longtext"]
        }
      }
    }
  }
}
