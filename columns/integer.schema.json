{
  "$id": "columns/integer.schema.json",
  "$merge": {
    "source": { "$ref": "_base.schema.json" },
    "with": {
      "properties": {
        "type": {
          "type": "string",
          "enum": ["integer"]
        },
        "unsigned": {
          "type": "boolean"
        }
      }
    }
  }
}
